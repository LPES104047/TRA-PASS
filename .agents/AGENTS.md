# Project Rules

## Git & Backup Policy
- **DO NOT** run `git push` or push code to GitHub directly.
- **DO NOT** execute any deployment or remote backup commands.
- **ALWAYS** prepare and display the exact Git commands (e.g., `git add`, `git commit`, `git push`) for the user to copy and execute manually, ensuring the user retains full control over repository updates.
