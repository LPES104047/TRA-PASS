# 🚂 火車資訊看板專案交接文件 (HANDOVER.md)

本文件紀錄了截至目前為止（2026年7月8日）的所有功能實現、技術調整與最佳實踐，以便新對話視窗的 AI 助手能夠無縫接軌。

---

## 📋 專案概觀
本專案是一個基於 **Next.js (App Router) + Turbopack** 開發的即時火車時刻看板。
* **預設觀測站**：鶯歌站 (Yingge)
* **核心資料源**：TDX (交通部民營運輸資料) 即時 API
  * 若 TDX API 呼叫失敗，系統會自動降級讀取本機緩存的靜態資料 `/public/data.json`。
* **本機開發網址**：`http://localhost:3000`

---

## 🎨 主題與設計系統 (Themes)
專案內建三個精心設計的卡片主題，使用者可在右上角進行切換：
1. **主題一 (極簡霓虹風)**：深藍至亮藍漸層背景，卡片為毛玻璃發光邊框，時間與資訊採上下分行排版。
2. **主題二 (精緻商務票卡)**：高質感淺灰色卡片，帶有模擬打孔的分割線與動態進度條。
3. **主題三 (賽博龐克儀表板)**：極致深色背景，搭配螢光綠、天藍色的亮色字體，左側帶有動態發光指示線。

---

## 🛠️ 近期重大技術調整與修復 (重點關注)

### 1. 🕒 誤點時間顯示邏輯 (防止視覺數學錯覺)
* **舊問題**：僅顯示紅色誤點時間時，使用者會混淆那是「原本時間」還是「延誤時間」，造成跟倒數計時對不上的錯覺。
* **新設計**：當火車發生誤點（`delay > 0`）時，出發與抵達時間一律並列顯示：
  * **`原本時間（劃掉並半透明）` `實時誤點時間（紅色警示）`**
  * 例如：`<del>12:49</del> <span style="color:#FF6B6B">12:50</span>`
  * 適用於所有三個主題。

### 2. 🛡️ 車票「鐵壁防禦」排版 (Theme 2 & 3)
* **舊問題**：加入雙時間後字串變長，在窄螢幕手機上會擠壓到旁邊的「車種標籤（如：區間車）」，導致標籤變形或變成直排字；若強行鎖死 `h3` 的 `nowrap` 則會在誤點時產生橫向溢出。
* **新設計**：
  * 對 `<h3>`（時間包覆區）設為 `white-space: normal` 與 `flex-wrap: wrap` 允許自然換行。
  * 對 `h3` 內部的子元件 `<span>`（即出發時間組與抵達時間組）個別套用 `white-space: nowrap`。這確保了「原定時間」與「誤點時間」永遠緊密連在一起，且在極窄螢幕下抵達時間可以安全地換行。
  * 對時間字體套用 CSS `clamp()` 進行動態無段縮小：
    * 主題二：`font-size: clamp(13px, 4vw, 18px)`
    * 主題三：`font-size: clamp(13px, 4vw, 24px)`
  * 對「車種標籤」套用 `flex-shrink: 0` 和 `white-space: nowrap`，確保標籤外觀永不變形。
  * 啟用 `.card-header` 的 `flex-wrap: wrap`，並對標籤加上 `margin-left: auto`。當螢幕窄到極限時，標籤會整塊安全地滑落至下一行靠右對齊，絕對不超出卡片或擠壓文字。

### 3. 🏹 目的地箭頭 (⟶) 垂直對齊修正
* **修正**：修正了中文字基準線導致箭頭偏下（下飄）的視覺問題。
* **對齊設定**：
  * 主題二與主題三的箭頭皆套用 `position: relative; top: -6px;`，使其在視覺上完美懸浮於時間數字的正中央。

### 4. 🚄 區間車背景動畫 (TrainAnimation.js) 雛形重塑與物理細節
* **車型雛形**：從原本保留座的 EMU3000 改為更貼近 TPASS 通勤日常的 **EMU900 型區間車**。
  * **車體結構**：採用銀灰色金屬漸層不鏽鋼車身、車門與車窗佈局改為通勤電聯車特有的多車廂多門窗配置，且窗戶增加 12% 半透明對角玻璃反光線以增強光澤。
  * **車頂集電弓**：車頂新增高精細度的雙受電弓/集電弓鋼骨結構。
  * **車底轉向架與輪胎**：車底新增機械轉向架與雙黑色輪胎，使行駛動作更具鐵道真實感。
  * **車頭設計**：重塑為圓潤的短車頭，並搭配 EMU900 特有的草綠色微笑波浪腰帶與黑鏡面面罩，底部加裝了排障器與連結器。
* **動畫方向與動態軌跡 (進站停靠頓挫感)**：
  * **進站停靠模擬**：動畫總長度拉長至 3s。列車並非等速通過，而是會以物理感十足的曲線前進：快速進站 ⟶ 減速至月台中央完全停靠（時鐘 40% 至 60% 處，此時首頁狀態立即無縫 Swap 更新） ⟶ 啟動並加速離站。
  * **方向動態化**：火車行進方向由出發地至目的地的「南北向」決定。
    * **北上 (Northbound)**：列車由左至右 (`ltr`) 開過。
    * **南下 (Southbound)**：列車由右至左 (`rtl`) 開過。
  * **流暢更新體驗**：為確保操作流暢性，更改「出發站」時會立即更新不拖沓；只有在更改「抵達站」與「交換起迄站」時才會觸發 EMU900 的進站停靠動畫。
* **效能與防裁切**：
  * 移除 `filter: blur` 與 `filter: drop-shadow`，啟用 `will-change: transform` 實現 GPU 60fps/120fps 順暢運動。
  * 將 SVG viewBox 調整為 `-1500 0 3100 220` 並設定 `.svg-emu900` 為 `overflow: visible`，確保車輪與車尾完全不被裁剪閃爍。
  * 透過 React 原生 `onAnimationEnd` 事件管理動畫生命週期。

---

## 📂 核心檔案清單
* `app/page.js`：首頁邏輯、資料串接、計時器與狀態管理。
* `app/api/trains/route.js`：後端 API 路由，負責向 TDX 抓取即時資料並解析。
* `app/components/Theme1.js`：主題一（霓虹風）卡片組件。
* `app/components/Theme2.js`：主題二（票卡風）卡片組件。
* `app/components/Theme3.js`：主題三（賽博風）卡片組件。
* `app/components/TrainAnimation.js`：背景 EMU900 動態區間車組件。
* `public/data.json`：本機備用班表資料。

---

## 🎯 新助理接手提示
當您在新的對話中啟動時，可以直接閱讀本文件 `HANDOVER.md`。
目前程式碼處於極度健康且穩定的狀態，可在此基礎上直接進行新功能擴充或設計微調。
